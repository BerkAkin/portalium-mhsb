<?php

namespace diginova\mhsb\controllers\console;

use Yii;


/**
 * RBAC console controller.
 */
class RbacController extends DefaultController
{

    public function actionAddPermissions(){

        /* $auth = Yii::$app->authManager;

        $createSample = $auth->createPermission('sample-samples-create');
        $createSample->description = 'User can create sample record.';
        $auth->add($createSample);
        echo "	->Sample Samples Create Added";
        echo "\n";

        $updateSample = $auth->createPermission('sample-samples-update');
        $updateSample->description = 'User can update sample record.';
        $auth->add($updateSample);
        echo "	->Sample Samples Update Added";
        echo "\n";
        $manageSample = $auth->createPermission('sample-samples-manage');
        $manageSample->description = 'User can manage sample records.';
        $auth->add($manageSample);
        echo "	->Sample Samples Manage Added";
        echo "\n";
        $viewSample = $auth->createPermission('sample-samples-view');
        $viewSample->description = 'User can view sample records.';
        $auth->add($viewSample);
        echo "	->Sample Samples View Added";
        echo "\n";
        $deleteSample = $auth->createPermission('sample-samples-delete');
        $deleteSample->description = 'User can delete sample records.';
        $auth->add($deleteSample);
        echo "	->Sample Samples Delete Added";
        echo "\n"; */


    }
}